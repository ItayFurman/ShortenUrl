@model List<URL>

@{
    ViewData["Title"] = "list of Short Link";
}

<h1>@ViewData["Title"]</h1>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Url</th>
            <th>Short</th>
            <th>Clicks Count</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model) {
            <tr>
                <td>@item.FullUrl</td>
                <td>
                    <a href="@(Context.GetFullDomain()+"/s/"+@item.ShortUrl)" target="_blank">
                        @item.ShortUrl</a>
                </td>
                <td>@item.NumOfRequest</td>
            </tr>
        }
    </tbody>
</table>